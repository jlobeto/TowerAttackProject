/// <summary>
/// AcceptOrDecline > 1 ok button and one close(or cancel)
/// BasePopup > 1 button (ok)
/// </summary>
public enum PopupsID
{
    AcceptOrDecline,
    BasePopup,
    Shop,
    StatUpgradeShop,
    LevelEndPopup
}
